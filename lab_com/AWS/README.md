This readme is for the code relevant to the AWS jobs queue

The file aws_lambda.py should be pasted into the lambda function page on AWS. 

To test the AWS configuration, run python3 auto_test_endpoints.py on your local machine.